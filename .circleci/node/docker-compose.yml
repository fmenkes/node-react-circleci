version: '3'

services:
  web:
    image: ${DOCKER_IMAGE}
    container_name: web
    environment:
      - MONGO_DB_URI=mongodb://mongo:27017/test-db
    command: yarn test:integration

  mongo:
    image: circleci/mongo
    expose:
      - "27017"
