version: '3'

services:
  web:
    image: ${DOCKER_IMAGE}
    container_name: web
    environment:
      - MONGO_DB_URI=mongodb://mongo:27017/test-db
    command: yarn test:integration

  mongo:
    image: mongo:3.4.10-jessie
    expose:
      - "27017"
